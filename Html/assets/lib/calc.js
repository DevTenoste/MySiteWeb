var ButtonVisibility = (ButtonCard.Visibility)
var Button = (ButtonCard)
if (Button) {
    onclick (
     ButtonVisibility = hidden
    )
   

}

var Commentaires = (avis)
WritableStream
Element
Document.call ( this.Base-de-données.sql)

/*
Pour une base de données, l'essentiel est d'ajouter un document sql avec les en-têtes requis,
on ajoute un document sur java script avec Document
*/

var Commentaires = avis + username + Date
